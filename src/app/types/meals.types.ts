export type MealType = {
  slug: string;
  title: string;
  image: string;
  summary: string;
  creator: string;
};

export type MealDetailsType = {
  slug: string;
  title: string;
  image: string;
  summary: string;
  creator: string;
  creator_email: string;
  instructions: string;
};
